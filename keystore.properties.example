# 复制本文件为 keystore.properties 并填入自己的签名信息（keystore.properties 已被 .gitignore 忽略）
#
# 先生成一个签名密钥（有效期约 27 年，妥善保管，丢了就无法覆盖安装升级）：
#   toolchain\jdk\bin\keytool.exe -genkeypair -v -keystore release.jks -alias 2048 ^
#     -keyalg RSA -keysize 2048 -validity 10000
#
# 不配置这个文件也能构建：release 包会用 debug 证书签名，照样能装到手机上用。

storeFile=release.jks
storePassword=在这里填密码
keyAlias=2048
keyPassword=在这里填密码
